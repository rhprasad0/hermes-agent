FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

COPY pyproject.toml ./
COPY codex_bridge ./codex_bridge

RUN pip install --no-cache-dir .

EXPOSE 8001

CMD ["uvicorn", "codex_bridge.main:app", "--host", "0.0.0.0", "--port", "8001"]
